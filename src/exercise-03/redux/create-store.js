/*
Import the transactions reducer and fetchTransactionsAsync 
from transactions-slice.js

Create a store with configureStore(). Pass in a reducer config
where there is a users slice and a transactions slice. 
Like this: 
{
  reducers: {
    users: usersReducer, transactions: transactionsReducer
  }
}

Dispatch calls to fetchUsersAsync() and fetchTransactionsAsync()

export the store by default
*/
